<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MessengerApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/aquamarine</item>
        <item name="colorPrimaryVariant">@color/aquamarine</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Message">
        <item name="tint">@color/black_700</item>
        <item name="android:textColor">@color/black_700</item>
        <item name="number_color">@color/black_700</item>
    </style>

    <!-- TabLayout Theme -->
    <style name="Theme.MessengerApp.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/channels_tab_indicator_color</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">15dp</item>
        <item name="tabPaddingEnd">15dp</item>
        <item name="tabTextAppearance">@style/Theme.MessengerApp.TabLayout.TextAppearance</item>
    </style>

    <!-- TabLayout TextAppearance -->
    <style name="Theme.MessengerApp.TabLayout.TextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">22sp</item>
    </style>

</resources>